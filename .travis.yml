
language: node_js
node_js:
  - "stable"
cache:
  directories:
    - "node_modules"
script:
  - npm run build
  - npm run coveralls
after_script:
  - |
    # Upload coverage to coveralls
    npm install --save-dev coveralls@2.10.0
    coveralls < ./coverage/lcov.info